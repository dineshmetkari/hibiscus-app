<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Styles for the light theme -->
    <style name="HibicusApp.Theme.ActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/title_background_color</item>
        <item name="android:titleTextStyle">@style/HibicusApp.Theme.ActionBar.TitleTextStyle</item>
        <item name="android:icon">@drawable/ic_launcher</item>
        <item name="selectableItemBackground">@drawable/action_item_background</item>
    </style>
    <style name="HibicusApp.Theme.ActionBar.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/title_text_color</item>
    </style>
</resources>